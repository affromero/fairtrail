#!/usr/bin/env bash
# Development convenience — runs the canonical CLI from apps/web/public/
# For production installs, the CLI lives at ~/.local/bin/fairtrail
exec bash "$(dirname "$0")/../apps/web/public/fairtrail-cli" "$@"
